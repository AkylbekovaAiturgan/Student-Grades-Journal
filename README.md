Название проекта: «Журнал оценок студентов»
Основная идея: Пользователь вводит имя, баллы за экзамен(промежуточный и финальный) и компьютер выводит итоговый балл(промежуточный 40% и финальный 60%), затем у пользователя появляется выбор «продолжить список» или «показать весь список студентов» с именами студентов и итоговыми баллами.



Как работает программа (каждая строка кода):
1)students = []: Это **список*. Квадратные скобки означают, что мы создали пустой "контейнер", куда будем складывать данные о каждом студенте.
2)def get_valid_score(prompt):- это функция с именнем 
-prompt- строка, которая будет выведена на экран перед ожиданием ввода, это как будущий вопрос, который мы еще не ввели
3) while True: Создает бесконечный цикл. Зачем? Чтобы программа не выключалась, если пользователь ввел ерунду, а спрашивала снова и снова.
4) try: "Попробуй выполнить этот код"
5) score = int(input(prompt))- озночает показать пользователю сообщение (prompt),
взять введённое значение, превратить его в число и сохранить в переменную score
6) 0 <= score <= 100-значение score находится между 0 и 100 включительно
7) return score-используется только внутри функции, означает: сохранить значение и завершить функцию
8) else:
                print('Ошибка! Такой оценки не существует.')- если числе меньше 0 или больше 100, цикл просит заново вести число
9) except ValueError: 
               print('Ошибка! Введите целое число.')
except ValueError -это «подушка безопасности» для твоего кода. Она нужна, чтобы программа не «вылетала» с ошибкой, если пользователь ввел ерунду вместо чисел. Если ввел не число, а букву или символы

Основная логика приложения
10) def student_app():- функция с именнем
    print("\n" + "="*40)
    print("ИНСТРУКЦИЯ:")
    print("+ — Добавить студента")
    print("= — Показать список и выйти")
    print("="*40) 
\n -перенос строки
11) while True:-создаем новую функцию 
12) name = input('\nНапишите имя студента: ') -озночает «Сначала перейди на новую строку, попроси ввести имя, и сохрани его в переменную name»
13) midterm = get_valid_score('Введите балл за промежуточный экзамен : ')
        final = get_valid_score('Введите балл за финальный экзамен : ')
наши переменные переносятся на первую функцию «get_valid_score» и проверят от ошибок
14)        w = round(midterm * 0.4)
        b = round(final * 0.6) 
мы берем новые переменные w,b и спомощью метода round()
-round()- Функция round() в Python используется для округления чисел до ближайшего целого или до определенного количества знаков после запятой.
Например:
a=4.5
b=8.3
c=a+b
print(round(c))

выводит  13
округляем наши числа
15)  total_score = w + b – суммируем наши переменные и получаем итоговый балл
16)   students.append({"name": name, "final_score": total_score})
     .append() — метод, который добавляет новый элемент в конец нашего списка.
    { "name": name, ... }: Это словарь. Фигурные скобки означают структуру "Ключ: Значение". Это как анкета: поле "Имя" — значение из переменной.
•	Ты добавляешь одного студента в список students
•	{} фигурные скобки-Это означает словарь (dictionary)
•	: двоеточие – озночает что связывает ключ и значение
"Создай объект (словарь), где имя = значение переменной name, итоговый балл значение total_score"
17) print(f'Промежуточный (40%): {w} | Финальный (60%): {b}')
        print(f'Итоговый балл: {total_score}')
выводит промежуточные баллы, финальные баллы и итоговый балл
18) print("\nЧто дальше?")
Затем нам задают вопрос, продолжить или закончить
19) choice = input("Выберите действие (+ или =): ")- просят выбрать одно из значений
 20)  if choice == '=': если это «=», то мы сразу заканчиваем цикл 
            Break
21) student_app()- запускаем функцию
22) print("\n======Список всех студентов======")
23)for s in students:*: Цикл "для каждого". Он берет по очереди каждого студента (s) из нашего списка students. 
f"{s['name']}: {s['final_score']}": Это f-строка. Буква f перед кавычками позволяет вставлять значения переменных прямо внутрь текста с помощью фигурных скобок {}.
